class MyClass {
  Init(): boolean {
    volatile test: int

    test := Init //@error - syntax (missing argument list in method call)

    return false    // this is a semantic ERRORO not syntax, this is sooo valid. cuz init can be a variable
  }
}

main() : int {
  print(MyClass().Init())
  return 0
}